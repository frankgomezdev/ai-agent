[project]
name = "ai-agent"
version = "0.1.0"
description = "A small Python CLI agent that uses an LLM with function calling to list, read, write, and run Python files inside a sandboxed working directory."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openai==2.44.0",
    "python-dotenv==1.1.0",
]
